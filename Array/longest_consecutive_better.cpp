#include<bits/stdc++.h>
using namespace std;
int consequitive(vector<int> arr){
    sort(arr.begin(),arr.end());
    int longest = 1;
    int count = 0;
    for(int i =1;i<arr.size();i++){
        if(arr[i] == arr[i-1]) continue;
        else{
            if()
        }
    }
}
int main(){

}